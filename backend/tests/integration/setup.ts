import { beforeAll, afterAll } from 'vitest';

beforeAll(async () => {
  // Configuração inicial dos testes de integração será adicionada
  // nas próximas tarefas, quando o Prisma for configurado.
});

afterAll(async () => {
  // Limpeza do ambiente de testes de integração.
});
