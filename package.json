{
  "name": "desafio-mupi-systems",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema web de agendamento para clínica odontológica - Desafio Mupi Systems",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "lint:fix": "npm run lint:fix -w backend && npm run lint:fix -w frontend",
    "format": "prettier --write \"**/*.{ts,tsx,mjs,js,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,mjs,js,json,md,yml,yaml}\"",
    "type-check": "npm run type-check -w backend && npm run type-check -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "prettier": "^3.5.3",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
